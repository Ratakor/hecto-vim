- delete doesn't delete the full selection (last is missing), same for yank
- Move cursor on undo/redo
- goto functions should work in visual mode
- add Space+p/Space+P/Space+y that uses system clipboard instead of editor clipboar
- pairing brace
- automatic indentation
- add w/e commands for word selection
- Refresh or remove help message
- Handle tab rendering correctly
- Add Ctrl-O and Ctrl-I that allow to jump to the previous/next location
- LSP support
- Rework syntax highlithing and use gruvbox dark theme by default?
